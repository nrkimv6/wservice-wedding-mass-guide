# Wedding Mass Guide 미구현 기능 구현 계획

> 작성일: 2026-01-12
> 대상 프로젝트: wedding-mass-guide
> 상태: ✅ Phase 1-5 핵심 기능 완료 (2026-01-13)
> PRD 참조: wedding-mass-prd.md

---

## 개요

혼배미사 순서지 웹앱 PRD 대비 현재 구현 상태를 분석하고 미구현 기능들의 구현 계획을 수립합니다.

### 현재 완료된 기능 (Phase 1 MVP)

| 기능 | PRD 섹션 | 상태 |
|------|----------|------|
| Welcome/Guide Screen | 3.1.1 | ✅ 완료 |
| Navigation (prev/next/swipe/keyboard) | 3.1.2 | ✅ 완료 |
| Step Content Display | 3.1.3 | ✅ 완료 |
| Text Size Control (5단계) | 3.1.4 | ✅ 완료 |
| Table of Contents | 3.1.5 | ✅ 완료 |
| Theme Selection (4종) | 3.1.6 | ✅ 완료 |
| massSteps 데이터 (32단계/18단계) | Appendix A | ✅ 완료 |

---

## 구현 항목

### Phase 1 잔여 (Attendee Features)

| 우선순위 | 항목 | PRD 섹션 | 난이도 |
|:-------:|------|----------|:------:|
| P1 | Announcement Banner | 3.1.7 | 낮음 |
| P1 | Optional Prayer 토글 적용 | 3.2.4 효과 | 중간 |
| P2 | Direct Step URL 지원 | 4.4 | 낮음 |

### Phase 2 (Admin Basic)

| 우선순위 | 항목 | PRD 섹션 | 난이도 |
|:-------:|------|----------|:------:|
| P0 | Admin 라우팅 구조 | 4.4 | 중간 |
| P0 | Admin Authentication (password) | 3.2.1 | 중간 |
| P0 | Mass Setup (기본 정보) | 3.2.2 | 중간 |
| P0 | Hymn Configuration | 3.2.3 | 중간 |
| P0 | Prayer Toggle (Gloria/Alleluia) | 3.2.4 | 낮음 |
| P0 | QR Code Generation | 3.2.6 | 중간 |
| P1 | Custom Announcements | 3.2.5 | 중간 |

### Phase 3 (Admin Advanced)

| 우선순위 | 항목 | PRD 섹션 | 난이도 |
|:-------:|------|----------|:------:|
| P1 | Preview Mode | 3.2.7 | 낮음 |
| P2 | Mass History | 3.2.8 | 중간 |
| P2 | Dashboard | 3.2.9 | 중간 |
| P2 | Mass Template 복사 | 3.2.2 | 낮음 |

### Phase 4 (Enhancements)

| 우선순위 | 항목 | PRD 섹션 | 난이도 |
|:-------:|------|----------|:------:|
| P1 | **관리자 원격 동기화** | 신규 | 높음 |
| P3 | PWA Offline 지원 | 4.1 | 높음 |
| P3 | Dark Mode | Phase 4 | 중간 |
| P3 | Analytics (scan count) | 3.2.9 | 높음 |

---

## 기술적 고려사항

### 데이터베이스 선택
- ~~**옵션 1**: Cloudflare D1 (sacred-hours, memo-alarm에서 사용)~~
- **옵션 2**: Supabase (line-minder에서 사용)
- **권장**: Supabase - auth_worker 통합, Realtime으로 실시간 동기화, line-minder 패턴 재사용

### 데이터 모델 (PRD 5.1 기반)
```typescript
// MassConfiguration
- id: string (UUID)
- createdAt, updatedAt: Date
- date, time, churchName, brideName, groomName, celebrantName
- hymns: { entrance, responsorial, offertory, communion, recessional }
- prayers: { gloria: boolean, alleluia: boolean }
- announcements: Announcement[]
- theme: string
- isActive: boolean
```

### URL 구조 구현
```
/                           - Landing/guide (현재 구현됨)
/mass/[massId]              - 특정 미사 뷰 (동적 성가/기도문)
/mass/[massId]/step/[step]  - 직접 스텝 링크
/admin                      - 관리자 로그인
/admin/dashboard            - 대시보드
/admin/mass/new             - 새 미사 생성
/admin/mass/[massId]        - 미사 편집
/admin/mass/[massId]/qr     - QR 코드
```

### QR 코드 생성
- `qrcode` 라이브러리 사용
- PNG 다운로드 지원 (300dpi 이상)
- URL 복사 버튼

### 인증 방식
- **auth_worker 통합** (Google/Kakao OAuth)
- 중앙 집중형 인증 서비스 사용 (auth.woory.day)
- Supabase Auth로 세션 관리
- 참고: `common/docs/guide/auth-worker-integration-guide.md`

**auth_worker 앱 등록 필요**:
```typescript
// D:\work\project\tools\auth-worker\src\config.ts
{
  appId: 'wedding-mass',
  origins: [
    'https://wedding-mass.woory.day',
    'http://localhost:5173'
  ],
  callbackPath: '/auth/callback'
}
```

### 관리자 원격 동기화 (Live Sync)

**2가지 진입 방식**

| 방식 | URL | 설명 |
|------|-----|------|
| **1. 그냥 시작** | `/` | 예식정보 없이 순서만 탐색 (현재 구현됨) |
| **2. 관리자 미사** | `/mass/[massId]` | 관리자 설정 반영 (성가, 장소, 동기화 등) |

```
방식 1: 그냥 시작 (기본)
┌──────────────────────────────────────┐
│ 누구나 접속 → 가이드 → 순서 탐색    │
│ - 성가: placeholder만 표시          │
│ - 정보 페이지: 없음                  │
│ - 동기화: 없음                       │
└──────────────────────────────────────┘

방식 2: 관리자 미사
┌──────────────────────────────────────┐
│ 관리자: /admin → 예식정보 입력 → QR │
│ 하객: QR 스캔 → /mass/abc123        │
│ - 성가: 관리자 설정값 표시           │
│ - 정보 페이지: 장소, 날짜, 신랑신부  │
│ - 동기화: 관리자와 실시간 연동 가능  │
└──────────────────────────────────────┘
```

**기능 개요 (방식 2)**
- 관리자가 현재 보고 있는 단계를 QR로 접속한 모든 하객에게 실시간 동기화
- 관리자가 설정한 미사 정보(성가, 상세정보, 전례시기, 테마)가 하객에게 반영
- 인터넷 장애 시 경고 표시 → 확인 → 동기화 모드 활성화/비활성화

**관리자 설정 항목**

| 카테고리 | 항목 | 설명 |
|----------|------|------|
| **성가** | 입당성가 | 번호 + 제목 + 페이지 |
| | 화답송 | 텍스트 입력 / "주보 참조"(기본) / 미표시 |
| | 봉헌성가 | 번호 + 제목 + 페이지 |
| | 영성체송 | 번호 + 제목 + 페이지 (**복수 가능**, 없으면 미표시) |
| | 파견성가 | 번호 + 제목 + 페이지 |
| | 축가 | 번호 + 제목 + 페이지 (없으면 미표시) |
| **상세정보** | 장소 | 성당/예식장 이름 |
| | 날짜 | YYYY-MM-DD |
| | 시간 | HH:MM |
| | 신랑 | 이름 |
| | 신부 | 이름 |
| | 주례사제 | 이름 (선택) |
| **전례시기** | 프리셋 | 연중/대림/사순/부활 |
| | 대영광송 | ON/OFF (사순·대림 시 OFF) |
| | 알렐루야 | ON/OFF (사순 시 OFF) |
| **테마** | 기본 테마 | ivory-gold/white-rose/cathedral/sage |
| **보기 모드** | 기본 단계 | 32단계(세분화) / 18단계(묶음) |

**성가 표시 위치 및 동적 연동**

성가는 **두 곳**에 표시됩니다:

| 위치 | 표시 내용 | 용도 |
|------|----------|------|
| **정보 페이지** | 전체 성가 목록 (5종) | 미리 확인, 성가책 준비 |
| **해당 단계** | 해당 성가만 (강조 표시) | **지금 부를 성가 안내** |

**현재 구현 상태**:
```typescript
// massSteps.ts - 현재는 placeholder만 있음
hymn: { type: 'entrance', label: '입당성가' }  // ← 번호/제목/페이지 없음!
```

**변경 필요**:
- HymnBox 컴포넌트가 `hymn.type`을 보고 관리자 설정에서 해당 성가 정보를 가져와 표시
- 관리자가 설정한 **번호 + 제목 + 페이지**가 해당 단계에 동적으로 표시

```
동작 흐름:
┌──────────────────┐     ┌──────────────────┐     ┌──────────────────┐
│ 관리자 설정      │ ──→ │ MassConfiguration│ ──→ │ HymnBox 표시     │
│ 입당: 152번, 87p │     │ hymns.entrance   │     │ 2단계에서 표시   │
└──────────────────┘     └──────────────────┘     └──────────────────┘
```

**해당 단계 표시 예시 (2. 입당성가)**:
```
┌─────────────────────────────────────────┐
│ 2. 입당성가                    🧍 기립  │
├─────────────────────────────────────────┤
│                                         │
│  주례사제가 제대로 나아가는 동안        │
│  함께 부르는 성가입니다.                │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │ 🎵 입당성가                       │  │
│  │                                   │  │
│  │   152번 - 다함께 노래해           │  │  ← 관리자 설정값
│  │   📖 성가책 87페이지              │  │  ← 비신자에게 핵심!
│  └───────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```

**성가 슬롯과 단계 매핑**:

| 성가 슬롯 | 표시되는 단계 | massSteps id |
|----------|--------------|--------------|
| entrance (입당) | 2. 입당성가 | 2 |
| responsorial (화답송) | 10. 화답송 | 10 |
| offertory (봉헌) | 20. 예물 준비 | 20 |
| communion (영성체) | 29. 영성체 | 29 |
| recessional (파견) | 32. 파견 | 32 |
| wedding (축가) | 18. 면사포 예식 (또는 별도) | TBD |

**하객 정보 페이지 (Info Page)**

하객도 미사 정보를 확인할 수 있는 별도 페이지 제공:
- 헤더의 정보 아이콘(ℹ️)으로 접근
- 또는 IntroScreen에서 "미사 정보 보기" 버튼

```
┌─────────────────────────────────────────┐
│ [←] 혼배미사 정보                       │
├─────────────────────────────────────────┤
│                                         │
│  💒 명동대성당                          │
│  📅 2026년 2월 14일 (토) 14:00         │
│                                         │
│  👰 신부: 김영희                        │
│  🤵 신랑: 홍길동                        │
│  ⛪ 주례: 김바오로 신부                 │
│                                         │
│  ─────────────────────────────────────  │
│                                         │
│  🎵 성가 안내                           │
│  ┌───────────────────────────────────┐  │
│  │ 입당    │ 152번 │ 📖 87p         │  │
│  │ 화답    │ (주보 참조)             │  │
│  │ 봉헌    │ 234번 │ 📖 142p        │  │
│  │ 영성체  │ 312번 │ 📖 189p        │  │
│  │         │ 415번 │ 📖 245p        │  │ ← 복수 가능
│  │ 파견    │ 401번 │ 📖 231p        │  │
│  └───────────────────────────────────┘  │
│  ※ 축가 없음 (설정 안됨)                │
│                                         │
│  ℹ️ 사순시기로 대영광송이 생략됩니다.   │
│                                         │
└─────────────────────────────────────────┘
```

**Progress Bar (페이지 진행률)**

기존: `4/32 인사` → 총 페이지수 제거, progress bar로 대체

```
기존:
┌─────────────────────────────────────┐
│ [≡]  ◀  4/32 인사  ▶   [작게][크게] │
└─────────────────────────────────────┘

변경:
┌─────────────────────────────────────┐
│ [≡] [ℹ️]    인사      [−] [+]      │
│ ████████░░░░░░░░░░░░░░░░░░░░░ 12%  │ ← progress bar
└─────────────────────────────────────┘

또는 (progress bar만, 퍼센트 없이):
┌─────────────────────────────────────┐
│ [≡] [ℹ️]    인사         [−] [+]   │
│ ████████░░░░░░░░░░░░░░░░░░░░░░░░░░ │
└─────────────────────────────────────┘
```

**관리자 수정 기능**

미사 진행 중에도 설정을 수정할 수 있는 기능:
- 실시간 성가 번호 수정 (예: 급하게 성가 변경)
- 공지사항 추가/수정
- 수정 시 연결된 모든 하객에게 즉시 반영

```
관리자 화면 (진행 중):
┌─────────────────────────────────────┐
│ [≡]  4단계: 인사           [⚙️ 수정]│ ← 수정 버튼
│ ████████░░░░░░░░░░░░░░░░░░░░░ 12%  │
│ 🔴 동기화 ON │ 연결: 47명          │
├─────────────────────────────────────┤
│ (미사 내용)                         │
│                                     │
│ [◀ 이전]              [다음 ▶]     │
└─────────────────────────────────────┘

수정 버튼 클릭 시:
┌─────────────────────────────────────┐
│ ⚙️ 빠른 수정                   [✕]  │
├─────────────────────────────────────┤
│ 🎵 성가 수정                        │
│   입당: [152] [다함께 노래해    ]   │
│   (다른 성가들...)                  │
│                                     │
│ 📢 공지 추가                        │
│   [                            ]    │
│   [+ 공지 추가]                     │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │        💾 저장 (즉시 반영)      │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

**설정 UI (관리자)**
```
┌─────────────────────────────────────────┐
│ 📋 혼배미사 설정                        │
├─────────────────────────────────────────┤
│                                         │
│ 📍 상세정보                             │
│ ┌─────────────────────────────────────┐ │
│ │ 장소: [명동대성당          ]        │ │
│ │ 날짜: [2026-02-14] 시간: [14:00]    │ │
│ │ 신랑: [홍길동    ] 신부: [김영희  ] │ │
│ │ 주례: [김바오로 신부      ] (선택)  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 🎵 성가                                 │
│ ┌─────────────────────────────────────┐ │
│ │ 입당: [152] [다함께 노래해  ] [87p] │ │
│ │ 화답: [●주보참조 ○직접입력 ○미표시] │ │
│ │       [텍스트 입력란 (직접입력 시)] │ │
│ │ 봉헌: [234] [주님께 드리는  ] [142p]│ │
│ │ 영성체:                             │ │
│ │   [312] [생명의 빵    ] [189p]      │ │
│ │   [+ 영성체송 추가]                 │ │
│ │ 파견: [401] [기쁜 소식      ] [231p]│ │
│ │ 축가: [   ] [              ] [   p]│ │
│ │       (비워두면 미표시)             │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ⛪ 전례시기                             │
│ ┌─────────────────────────────────────┐ │
│ │ [연중] [대림] [사순] [부활]         │ │
│ │ ─────────────────────────────────── │ │
│ │ 대영광송: [====●    ] ON            │ │
│ │ 알렐루야: [====●    ] ON            │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 🎨 테마                                 │
│ ┌─────────────────────────────────────┐ │
│ │ [●ivory] [○rose] [○cathedral] [○sage]│ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 📖 보기 모드                            │
│ ┌─────────────────────────────────────┐ │
│ │ [●32단계 상세]  [○18단계 간결]      │ │
│ │ ※ 하객에게 기본 표시되는 단계 수    │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │              💾 저장                 │ │
│ └─────────────────────────────────────┘ │
└─────────────────────────────────────────┘
```

**사용자 플로우**
```
[관리자]                         [하객들]
미사 진행 중                      QR 스캔 → 접속
   │                               │
동기화 모드 ON ────────────────────→ "관리자와 동기화 중" 표시
   │                               │
다음 단계 이동 ────────────────────→ 자동으로 같은 단계로 이동
   │                               │
[연결 끊김] ─────────────────────→ "연결이 끊겼습니다" 경고
   │                               │
동기화 모드 OFF ───────────────────→ 자유 탐색 모드로 전환
```

**기술 옵션**

| 옵션 | 장점 | 단점 |
|------|------|------|
| **Supabase Realtime** | DB와 통합, line-minder 경험 있음, 간단한 API | 외부 서비스 의존 |
| **Cloudflare Durable Objects** | Cloudflare 인프라 통합, 낮은 지연 | 복잡한 설정, 비용 발생 |
| **PartyKit** | 실시간 특화, 간단한 API | 별도 서비스 의존 |
| **Polling (fallback)** | 구현 간단, 안정적 | 실시간성 떨어짐 (2-5초 딜레이) |

**권장 구현**: Supabase Realtime + Polling Fallback
- Supabase Broadcast 채널로 실시간 단계 동기화
- 연결 실패 시 Polling으로 자동 전환

**UI 컴포넌트**
```
관리자 화면:
┌────────────────────────────────┐
│ [동기화 OFF] ← 토글 버튼       │
│ 연결된 하객: 0명               │
└────────────────────────────────┘

↓ 토글 ON 시

┌────────────────────────────────┐
│ [동기화 ON] 🟢                  │
│ 연결된 하객: 47명              │
│ ⚠️ 인터넷 연결 필요           │
└────────────────────────────────┘

하객 화면 (동기화 모드):
┌────────────────────────────────┐
│ 🔴 관리자와 동기화 중          │ ← 상단 배너
│ [자유 탐색으로 전환]           │
├────────────────────────────────┤
│ (현재 단계 내용)               │
│ ← → 버튼 비활성화              │
└────────────────────────────────┘

연결 끊김 시:
┌────────────────────────────────┐
│ ⚠️ 연결이 끊겼습니다           │
│                                │
│ 인터넷 상태를 확인해주세요.    │
│ 연결이 복구되면 자동으로       │
│ 다시 동기화됩니다.             │
│                                │
│ [자유 탐색 모드로 전환]        │
│ [재연결 시도]                  │
└────────────────────────────────┘
```

**데이터 모델 (Supabase)**

```sql
-- Supabase 테이블 스키마

-- 미사 설정 테이블
create table mass_configurations (
  id uuid primary key default gen_random_uuid(),
  user_id uuid references auth.users(id) on delete cascade,

  -- 상세정보
  church_name text not null,
  date date not null,
  time time not null,
  groom_name text not null,
  bride_name text not null,
  celebrant_name text,

  -- 성가 (JSONB)
  hymns jsonb default '{}',
  -- {
  --   entrance: { number: "152", title: "다함께 노래해", page: "87" },
  --   responsorial: "주보 참조" | null,
  --   offertory: { ... },
  --   communion: [{ ... }, { ... }],
  --   recessional: { ... },
  --   wedding: { ... } | null
  -- }

  -- 전례시기
  liturgical_season text default 'ordinary', -- ordinary, advent, lent, easter
  gloria_enabled boolean default true,
  alleluia_enabled boolean default true,

  -- 설정
  theme text default 'ivory-gold',
  view_mode text default 'detailed', -- detailed(32단계), merged(18단계)

  -- 실시간 동기화
  sync_enabled boolean default false,
  current_step integer default 1,

  created_at timestamptz default now(),
  updated_at timestamptz default now()
);

-- RLS 정책
alter table mass_configurations enable row level security;

create policy "Users can CRUD own masses"
  on mass_configurations for all
  using (auth.uid() = user_id);

-- 하객은 읽기만 가능 (QR 접근)
create policy "Anyone can read active masses"
  on mass_configurations for select
  using (true);

-- updated_at 자동 갱신 트리거
create trigger update_mass_configurations_updated_at
  before update on mass_configurations
  for each row execute function update_updated_at_column();
```

```typescript
// TypeScript 인터페이스
interface HymnEntry {
  number: string;
  title: string;
  page: string;
}

interface MassConfiguration {
  id: string;
  user_id: string;

  church_name: string;
  date: string;
  time: string;
  groom_name: string;
  bride_name: string;
  celebrant_name?: string;

  hymns: {
    entrance?: HymnEntry;
    responsorial?: string | null;
    offertory?: HymnEntry;
    communion?: HymnEntry[];
    recessional?: HymnEntry;
    wedding?: HymnEntry;
  };

  liturgical_season: 'ordinary' | 'advent' | 'lent' | 'easter';
  gloria_enabled: boolean;
  alleluia_enabled: boolean;

  theme: 'ivory-gold' | 'white-rose' | 'cathedral' | 'sage';
  view_mode: 'detailed' | 'merged';

  sync_enabled: boolean;
  current_step: number;

  created_at: string;
  updated_at: string;
}

// 전례시기별 기본값
const LITURGICAL_PRESETS = {
  ordinary: { gloria: true, alleluia: true },
  advent: { gloria: false, alleluia: true },
  lent: { gloria: false, alleluia: false },
  easter: { gloria: true, alleluia: true },
};
```

**Supabase Realtime 동기화**
```typescript
// 관리자: 단계 변경 시 브로드캐스트
const channel = supabase.channel(`mass:${massId}`);
channel.send({
  type: 'broadcast',
  event: 'step_change',
  payload: { step: currentStep }
});

// 하객: 단계 변경 수신
channel.on('broadcast', { event: 'step_change' }, ({ payload }) => {
  currentStep = payload.step;
});
```

---

## 구현 순서

### Phase 1 잔여 (3-4일) ✅ 완료 (2026-01-12)
1. [x] P0: Progress Bar로 헤더 개선
   - 기존 `4/32` 형식 제거
   - 가로 progress bar 추가 (퍼센트 없이)
   - 정보 아이콘(ℹ️) 버튼 추가
   - 커밋: `e0d373a` feat: add progress bar and info button to header
2. [x] P0: 하객 정보 페이지 (MassInfoPage)
   - 장소, 날짜, 시간, 신랑, 신부, 주례 표시
   - 성가 목록 (5종) 표시
   - 전례시기 안내 (생략예식 알림)
   - 헤더 ℹ️ 버튼으로 접근
   - 커밋: `00d70c0` feat: add mass information page for attendees
3. [x] P1: Announcement Banner 컴포넌트
   - 상단 배너 형태
   - localStorage로 dismiss 상태 저장
   - 커밋: `cf91bd8` feat: add announcement banner component
4. [x] P1: Optional Prayer 토글 효과
   - massSteps에 isOptional, optionalKey 필드 추가
   - 대영광송(step 7), 알렐루야(step 11) 조건부 표시
   - navigation 로직도 필터링된 단계만 표시
   - 커밋: `febdea8` feat: implement optional prayer toggle (Gloria/Alleluia)
5. [x] P2: Direct Step URL 지원
   - `/step/5` → `/?step=5` 리다이렉트
   - URL 파라미터로 직접 단계 접근
   - 유효성 검사 및 필터링된 단계만 허용
   - 커밋: `73635a5` feat: add direct step URL support
6. [x] P1: Wake Lock API (화면 꺼짐 방지)
   - 미사 시작 시 자동으로 wake lock 활성화
   - 페이지 visibility 변경 시 재획득
   - 페이지 이탈 시 자동 해제
   - 커밋: `c22cb6e` feat: implement Wake Lock API to prevent screen sleep

### Phase 2 Admin UI ✅ 완료 (2026-01-13)
1. [x] P0: Admin 라우팅 구조 생성
   - `/admin/+layout.svelte` (인증 guard placeholder)
   - `/admin/+page.svelte` (로그인 UI)
   - `/admin/dashboard/+page.svelte` (대시보드)
   - `/admin/mass/new/+page.svelte` (미사 생성 폼)
   - `/admin/mass/[massId]/+page.svelte` (미사 관리 및 QR)
   - 커밋: `2c2e876` feat: add admin pages UI (no backend yet)
2. [x] P0: Mass Setup 폼 UI
   - 상세정보 입력 (장소, 날짜, 시간, 신랑, 신부, 주례)
   - 커밋: `2c2e876` feat: add admin pages UI (no backend yet)
3. [x] P0: Hymn Configuration UI
   - 5개 성가 슬롯 (입당, 화답, 봉헌, 영성체, 파견)
   - 축가 (선택)
   - 영성체송 복수 지원
   - 화답송 3가지 옵션 (주보 참조 / 직접 입력 / 미표시)
   - 커밋: `2c2e876` feat: add admin pages UI (no backend yet)
4. [x] P0: Prayer Toggle UI
   - 전례시기 프리셋 버튼 (연중, 대림, 사순, 부활)
   - Gloria/Alleluia 개별 토글
   - 커밋: `2c2e876` feat: add admin pages UI (no backend yet)
5. [x] P0: Theme & View Mode Selection
   - 테마 선택 (4종)
   - 보기 모드 선택 (32단계 / 18단계)
   - 커밋: `2c2e876` feat: add admin pages UI (no backend yet)
6. [x] P0: QR Code Generation UI
   - qrcode 라이브러리 설치
   - QR 코드 생성 및 표시
   - PNG 다운로드 기능
   - URL 복사 기능
   - 미리보기 버튼
   - 커밋: `2c2e876` feat: add admin pages UI (no backend yet)

### Phase 2 Admin Backend ✅ 완료 (2026-01-13)
1. [x] P0: Supabase 설정
   - 프로젝트: 기존 wservice Supabase 사용 (line-minder와 동일)
   - `supabase/migrations/20260113_001_mass_configurations.sql` 생성
   - 커밋: `b02926a` feat: implement admin authentication with auth_worker
2. [x] P0: auth_worker 앱 등록
   - `wedding-mass` appId 추가 (config.ts)
   - ALLOWED_ORIGINS 업데이트 (wrangler.toml)
   - auth_worker 배포
   - 커밋: `482b950` feat: add wedding-mass app configuration (auth-worker)
3. [x] P0: Admin Authentication 구현
   - Google/Kakao 로그인 버튼
   - auth_worker 리다이렉트
   - Supabase 세션 관리
   - 인증 guard 활성화
   - 커밋: `b02926a` feat: implement admin authentication with auth_worker
4. [x] P0: Mass CRUD API 구현
   - Create/Read/Update/Delete endpoints (massService)
   - Supabase 연동
   - 폼 데이터 저장/불러오기
   - 커밋: `3ffe675` feat: implement Mass CRUD API with Supabase
5. [x] P1: Custom Announcements ✅ 완료 (2026-01-13)
   - AnnouncementManager 컴포넌트 (CRUD)
   - 순서 변경 (move up/down)
   - Admin 폼에 통합
   - 첫 번째 공지사항을 배너에 표시
   - 커밋: `0bbd61e` feat: implement custom announcements management

### Phase 3 Admin Advanced (1주) ✅ 완료 (2026-01-13)
12. [x] P1: Preview Mode ✅ 완료 (2026-01-13)
    - `/mass/[massId]` 라우트 생성
    - 미사 설정 DB 로드 및 표시
    - IntroScreen에 massInfo 표시
    - 설정된 theme/viewMode 자동 적용
    - localStorage 격리 (massId별)
    - 커밋: `3ec8cbc` feat: implement preview mode for attendee view
13. [x] P2: Mass History ✅ 완료 (2026-01-13)
    - 생성된 미사 목록 (예정/종료 구분)
    - 날짜순 정렬
    - 삭제 기능 (확인 다이얼로그)
    - 커밋: `224d37c` feat: implement remaining admin features
14. [x] P2: Dashboard ✅ 완료 (2026-01-13)
    - 현재/예정 미사 요약
    - 빠른 편집 접근
    - 커밋: `224d37c` feat: implement remaining admin features

### Phase 4 관리자 원격 동기화 (1-2주)
15. [x] P1: Supabase Realtime 설정 ✅ 완료 (2026-01-13)
    - Broadcast 채널 구현
    - Presence tracking (연결된 사용자 수)
    - realtimeSync.svelte.ts 스토어 생성
    - 커밋: `8a46ea4` feat: implement Supabase Realtime sync for attendees
16. [x] P1: 관리자 설정 UI ✅ 완료 (2026-01-13, Phase 2에서)
    - 상세정보 폼 (장소, 날짜, 시간, 신랑, 신부, 주례)
    - 성가 설정 (입당, 봉헌, 영성체, 파견, 축가)
    - 전례시기 프리셋 + 개별 토글 (대영광송, 알렐루야)
    - 테마 기본값 선택
    - 커밋: `2c2e876` feat: add admin pages UI (no backend yet)
17. [x] P1: 관리자 동기화 컨트롤 ✅ 완료 (2026-01-13)
    - SyncControl 컴포넌트 통합 완료
    - 관리자 전용 미사 뷰어 (/admin/mass/[massId]/view)
    - 동기화 ON/OFF 토글
    - 연결된 클라이언트 수 표시 (Presence 사용)
    - 현재 단계 브로드캐스트 (관리자 네비게이션 시 자동)
    - 커밋: `ce2c019` feat: integrate admin sync control and viewer
18. [x] P1: 하객 동기화 클라이언트 ✅ 완료 (2026-01-13)
    - SyncStatusBanner 컴포넌트
    - 자동 단계 이동
    - 자유 탐색 전환 옵션
    - 커밋: `8a46ea4` feat: implement Supabase Realtime sync for attendees
19. [x] P1: 연결 상태 처리 ✅ 완료 (2026-01-13)
    - 연결 끊김 감지
    - 오류 메시지 표시
    - 커밋: `8a46ea4` feat: implement Supabase Realtime sync for attendees
20. [x] P1: 관리자 실시간 수정 기능 ✅ 완료 (2026-01-13)
    - 진행 중 빠른 수정 버튼 (Edit2 아이콘)
    - QuickEditModal 컴포넌트
    - 성가 번호/제목/페이지 즉시 수정
    - 저장 시 DB 업데이트 및 즉시 반영
    - 커밋: `224d37c` feat: implement remaining admin features

### Phase 5 기타 ✅ 완료 (2026-01-13)
21. [x] P0: 데이터 익일 삭제 안내 ✅ 완료 (2026-01-13)
    - 관리자에게 "미사 데이터는 익일 자동 삭제됩니다" 안내
    - Dashboard에 안내 박스 표시
    - Mass 관리 페이지에 경고 섹션 추가
    - 커밋: `224d37c` feat: implement remaining admin features
    - **참고**: Supabase Edge Function 자동 삭제는 별도 구현 필요 (현재는 수동 삭제만 가능)
22. [x] P1: 화면 꺼짐 방지 (Wake Lock API) ✅ 완료 (Phase 1)
    - 미사 중 화면 자동 꺼짐 방지
    - 사용자가 끄기 전까지 유지
    - 커밋: `c22cb6e` feat: implement Wake Lock API to prevent screen sleep
23. [ ] P2: 시작 시 전체 다운로드 (오프라인 지원)
    - IntroScreen에서 "시작" 클릭 시 필요한 데이터 모두 다운로드
    - 이후 인터넷 끊겨도 사용 가능
    - Service Worker 캐싱
24. [ ] P2: 다크모드 (어두운 테마 추가)
    - 기존 4종 + 다크 테마 1종 추가
    - 성당 내 어두운 환경 대응
25. [ ] P3: Analytics

---

## 추후 고려사항

구현하면서 필요 시 결정할 항목들:

| 항목 | 설명 | 비고 |
|------|------|------|
| 축가 위치 선택 | 18단계(면사포) 외 다른 위치 | 현재는 18단계 고정 |
| 관리자 복수 지원 | 동시 편집 충돌 처리 | 현재는 단일 비밀번호 |
| 에러 처리 | 잘못된 QR, 만료된 미사 등 | 기본 안내 메시지 |
| 데이터 보관 연장 | 익일 삭제 외 옵션 | 현재는 익일 자동 삭제 |

---

## 파일 구조 (예상)

```
wedding-mass-guide/
├── src/
│   ├── routes/
│   │   ├── +page.svelte                  # 현재 (기본 뷰)
│   │   ├── login/
│   │   │   └── +page.svelte              # 로그인 페이지
│   │   ├── auth/
│   │   │   └── callback/
│   │   │       └── +page.svelte          # OAuth 콜백
│   │   ├── mass/
│   │   │   └── [massId]/
│   │   │       ├── +page.svelte          # 동적 미사 뷰
│   │   │       └── step/
│   │   │           └── [step]/
│   │   │               └── +page.svelte
│   │   └── admin/
│   │       ├── +layout.svelte            # 인증 guard
│   │       ├── +page.svelte              # 대시보드
│   │       └── mass/
│   │           ├── new/
│   │           │   └── +page.svelte
│   │           └── [massId]/
│   │               ├── +page.svelte      # 편집
│   │               └── qr/
│   │                   └── +page.svelte
│   └── lib/
│       ├── components/
│       │   ├── Header.svelte                 # 헤더 (progress bar 포함)
│       │   ├── ProgressBar.svelte            # 진행률 바
│       │   ├── MassInfoPage.svelte           # 하객 정보 페이지
│       │   ├── AnnouncementBanner.svelte     # 공지 배너
│       │   ├── LiveSyncBanner.svelte         # 하객 동기화 배너
│       │   ├── LiveSyncControl.svelte        # 관리자 동기화 컨트롤
│       │   ├── ConnectionWarning.svelte      # 연결 끊김 경고
│       │   ├── QuickEditModal.svelte         # 관리자 빠른 수정 모달
│       │   ├── GoogleLoginButton.svelte      # Google 로그인 버튼
│       │   ├── KakaoLoginButton.svelte       # Kakao 로그인 버튼
│       │   └── admin/                        # 관리자 컴포넌트
│       │       ├── MassForm.svelte           # 상세정보 폼
│       │       ├── HymnConfig.svelte         # 성가 설정 (5종)
│       │       ├── LiturgicalSeason.svelte   # 전례시기 + 생략예식 토글
│       │       ├── ThemeConfig.svelte        # 테마 기본값 설정
│       │       └── QRGenerator.svelte        # QR 코드 생성
│       ├── services/
│       │   └── supabase.ts               # Supabase 클라이언트
│       ├── stores/
│       │   ├── auth.svelte.ts            # 인증 상태 관리
│       │   └── liveSync.svelte.ts        # 동기화 상태 관리
│       ├── config.ts                     # 환경설정 (AUTH_WORKER_URL 등)
│       └── types/
│           └── mass.ts                   # MassConfiguration 타입
├── data/
│   └── migrations/
│       └── 001_mass_configurations.sql   # Supabase 스키마
├── .env                                  # 환경변수
└── wrangler.toml
```

---

## Cloudflare Pages Git 연동 배포 설정

### 작업 내용 (2026-01-12)

#### 1. GitHub 리포지토리 생성
- 리포지토리: `nrkimv6/wservice-wedding-mass-guide` (public)
- 생성일: 2026-01-12

#### 2. Cloudflare Pages 프로젝트
- 프로젝트명: `wservice-wedding-mass-guide`
- 도메인: `wservice-wedding-mass-guide.pages.dev`, `wedding-mass-guide.woory.day`
- 문제: Git Provider 연동 없음 (직접 배포로 생성됨)
- 해결: **Cloudflare Pages는 Git 통합을 더 이상 지원하지 않음**

#### 3. GitHub Actions 자동 배포 설정
로컬 디렉토리: `d:\work\project\service\wtools\wedding-mass-guide`

**워크플로우 파일**: `.github/workflows/deploy.yml`
```yaml
name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      deployments: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy dist --project-name=wservice-wedding-mass-guide
```

#### 4. 필요한 GitHub Secrets

| Secret 이름 | 값 | 상태 |
|-------------|-----|------|
| `CLOUDFLARE_ACCOUNT_ID` | `fd428eacb85e980d6f18cfb627b916de` | ⏳ 설정 필요 |
| `CLOUDFLARE_API_TOKEN` | (Cloudflare Dashboard에서 생성) | ⏳ 설정 필요 |

**API Token 생성 방법**:
1. https://dash.cloudflare.com/profile/api-tokens
2. "Create Token" → "Edit Cloudflare Workers" 템플릿 선택
3. Permissions에 Pages 권한 포함 확인
4. 생성된 토큰 복사
5. GitHub 리포지토리 → Settings → Secrets and variables → Actions → New repository secret

#### 5. 커밋 이력
```
f76cad9 chore: add GitHub Actions workflow for Cloudflare Pages deployment
9d6b39a (이전 커밋)
```

#### 6. 다음 단계
- [x] Cloudflare API Token 생성
- [x] GitHub Secrets 설정 (CLOUDFLARE_API_TOKEN, CLOUDFLARE_ACCOUNT_ID)
- [x] main 브랜치 push 테스트로 자동 배포 확인
- [x] 글자 크기 조절 버그 수정

---

## 버그 수정 이력

### 글자 크기 조절 버튼 작동 안 함 (2026-01-12)

**문제**:
- Header의 +/- 버튼 클릭 시 textSize 값은 변경되지만 실제 화면에 반영되지 않음
- `text-size-{textSizeStore.value}` 동적 클래스가 적용되지 않음

**원인**:
1. Svelte에서 템플릿 리터럴 방식의 동적 클래스(`text-size-{value}`)는 반응성 문제 발생
2. 값이 변경되어도 클래스가 교체되지 않고 유지됨
3. CSS 변수는 정의되어 있으나(`.text-size-1` ~ `.text-size-5`), 클래스가 적용되지 않아 효과 없음

**수정 내용**:

1. **+page.svelte (132번째 줄)**:
   ```svelte
   <!-- 수정 전 -->
   <div class="min-h-screen bg-background text-size-{textSizeStore.value}">

   <!-- 수정 후 -->
   <div class="min-h-screen bg-background"
        class:text-size-1={textSizeStore.value === 1}
        class:text-size-2={textSizeStore.value === 2}
        class:text-size-3={textSizeStore.value === 3}
        class:text-size-4={textSizeStore.value === 4}
        class:text-size-5={textSizeStore.value === 5}>
   ```
   - Svelte의 `class:` 디렉티브 사용
   - 조건부로 각 클래스를 추가/제거하여 반응성 보장

2. **HymnBox.svelte (15, 17번째 줄)**:
   ```svelte
   <!-- 추가: CSS 변수를 HymnBox에도 적용 -->
   <p class="font-medium" style="font-size: var(--content-font-size, 18px);">
   <p class="text-muted-foreground mt-2" style="font-size: calc(var(--content-font-size, 18px) * 0.85);">
   ```
   - 성가 박스 텍스트에도 글자 크기 조절 적용

**결과**:
- 글자 크기 버튼(+/-) 클릭 시 즉시 반영됨
- 1단계(14px) ~ 5단계(24px) 정상 작동
- 모든 컨텐츠(설명, 기도문, 성가)에 일관되게 적용

**커밋**:
- `258deac` - fix: apply text size CSS variable to HymnBox component
- `255a666` - fix: use Svelte class directive for text size reactivity

---

## 최종 완료 요약 (2026-01-13)

### ✅ 완료된 Phase

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 1 | MVP 기본 기능 (하객용) | ✅ 완료 |
| Phase 2 | Admin Backend | ✅ 완료 |
| Phase 3 | Admin Advanced (History, Dashboard) | ✅ 완료 |
| Phase 4 | 관리자 원격 동기화 | ✅ 완료 |
| Phase 5 | 데이터 삭제 안내, Wake Lock | ✅ 완료 |

### 주요 기능

**하객 (Attendee) 기능:**
- ✅ 미사 순서 탐색 (32단계/18단계)
- ✅ 텍스트 크기 조절 (5단계)
- ✅ 테마 선택 (4종)
- ✅ 목차 (Table of Contents)
- ✅ 실시간 동기화 (관리자 단계 자동 따라가기)
- ✅ 자유 탐색 모드 전환
- ✅ 공지사항 배너
- ✅ 미사 정보 페이지
- ✅ Wake Lock (화면 꺼짐 방지)

**관리자 (Admin) 기능:**
- ✅ Google/Kakao 로그인 (auth_worker)
- ✅ 미사 생성/수정/삭제
- ✅ 성가 설정 (입당/화답/봉헌/영성체/파견/축가)
- ✅ 전례시기 프리셋 (연중/대림/사순/부활)
- ✅ 대영광송/알렐루야 토글
- ✅ QR 코드 생성 및 다운로드
- ✅ 관리자 전용 미사 뷰어
- ✅ 실시간 동기화 컨트롤 (ON/OFF)
- ✅ 연결된 하객 수 표시 (Presence)
- ✅ 빠른 수정 기능 (진행 중 성가 수정)
- ✅ 미사 목록 (예정/종료 구분)
- ✅ 공지사항 관리

### 기술 스택
- **Frontend**: SvelteKit 2, Svelte 5, TypeScript
- **Backend**: Supabase (PostgreSQL, Realtime, Auth)
- **Authentication**: auth_worker (OAuth 통합)
- **Deployment**: Cloudflare Pages (GitHub Actions)
- **Styling**: Tailwind CSS 4

### 배포 정보
- **Production URL**: https://wedding-mass-guide.woory.day
- **GitHub**: https://github.com/nrkimv6/wservice-wedding-mass-guide
- **Auto-deploy**: GitHub Actions → Cloudflare Pages

### 남은 선택 사항 (P2-P3)
- [ ] P2: 오프라인 지원 강화 (Service Worker 캐싱)
- [ ] P2: 다크모드
- [ ] P3: Analytics (QR 스캔 수 등)
- [ ] 별도: Supabase Edge Function (익일 자동 삭제)

---

*최종 업데이트: 2026-01-13*
*상태: 실제 사용 가능한 Production-ready 상태*
