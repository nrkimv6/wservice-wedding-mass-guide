---
paths:
  - "frontend/**/*.svelte"
  - "frontend/**/*.tsx"
  - "frontend/**/*.vue"
  - "src/**/*.svelte"
  - "src/**/*.tsx"
  - "src/**/*.vue"
---

# UI 컴포넌트 구현 체크리스트

목록/그리드 형태의 UI 구현 시 아래 항목을 반드시 확인한다.

## 필수 (데이터 100건 이상 예상 시)
- **페이지네이션 또는 무한스크롤** — limit만 있고 skip/offset이 없으면 안 됨
- **이벤트 버블링 처리** — 중첩 클릭 영역(카드 > 버튼/체크박스)에 `e.stopPropagation()`
- **관계 데이터 이름 표시** — FK ID를 숫자로 표시 금지, 반드시 이름/경로 해석
- **벌크 액션** — 선택 기능이 있으면 벌크 처리(승인/삭제/분류 등) 제공
- **전체 선택/해제** — 벌크 액션이 있으면 전체 선택 버튼 필수
- **새로고침** — 데이터 변경 후 목록 갱신 수단 제공

## 아이콘 규칙
- **이모지 사용 금지** — UI에 이모지(유니코드 이모지, 텍스트 이모지) 직접 삽입 금지
- **아이콘 라이브러리 사용 필수** — 아이콘이 필요하면 `lucide-svelte`에서 import하여 사용
- 예: `import { Monitor, Calendar, Search } from 'lucide-svelte';`

## 기존 탭과 교차 검증
- 동일 모듈 내 유사 탭의 기능 목록과 대조
- 누락된 기능이 "의도적 제외"인지 "미구현"인지 명시
